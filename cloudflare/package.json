{
  "name": "ytimesy-cloudflare",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply ytimesy-feedback --local",
    "db:migrate:remote": "wrangler d1 migrations apply ytimesy-feedback --remote"
  },
  "devDependencies": {
    "wrangler": "^4.46.0"
  }
}
